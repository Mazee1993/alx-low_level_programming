#include "main.h"

/**
 * mul -  multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with the first
 * Return: The results multiplication between x param and y param
 */

int mul(int x, int y)
{
return (x * y);
}
